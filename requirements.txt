fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
numpy>=1.26
matplotlib>=3.8
huggingface_hub>=0.23
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.6                # lint: `ruff check .`
prometheus_client>=0.20  # tests only: validates /metrics with the official parser

# Real inference backend. On Apple Silicon build with Metal:
#   CMAKE_ARGS="-DGGML_METAL=on" FORCE_CMAKE=1 pip install llama-cpp-python
llama-cpp-python>=0.3.0
